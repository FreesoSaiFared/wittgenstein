# NotebookLM local promotion schema fixture

This fixture defines the local promotion artifact shape for NotebookLM provider evidence.

The fixture can allow executor-context entry for locally verified provider evidence, but it still grants no patch authority.

No live NotebookLM operation was performed.

```text
NotebookLM can inform; local artifacts decide.
```
