# NotebookLM captured-result fixture

This is a local synthetic fixture for the captured-result shape.

It does not come from NotebookLM.
It does not prove NotebookLM auth works.
It does not authorize implementation.

The fixture exists so local code can test how a future redacted `readonly-list` capture will be represented.

```text
NotebookLM can inform; local artifacts decide.
```
